import { createStyles, padding } from '../helpers/baseStyles';

const styles = createStyles({
  container: {
    backgroundColor: '#fff',
    flex: 1,
    justifyContent: 'center'
  },
  button: {
    padding: padding.xs,
    margin: padding.sm
  }
});

export default styles;
