import { createStyles, scale } from '../helpers/baseStyles';

const styles = createStyles({
  container: {
    backgroundColor: '#fff',
    flex: 1
  },

  suggestionContainer: {
    padding: scale(5)
  }
});

export default styles;
